baseURL: "http://localhost/"
title: "Infosec Notes"
theme: "PaperMod"
languageCode: "en-us"

params:
  author: "hoverdd"
  defaultTheme: auto
  disableSpecial1stPost: true
  ShowCodeCopyButtons: true

frontmatter:
  format: yaml

outputs:
  home:
    - HTML
    - RSS
    - JSON
